[build]
  publish = "public"
  functions = "netlify/functions"

# API functions
[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200

# SPA fallback: let the client handle routes
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

